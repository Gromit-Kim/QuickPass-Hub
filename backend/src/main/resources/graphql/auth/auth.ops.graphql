extend type Query {
    userInfo: UserInfo!
}

extend type Mutation {
    register(input: RegisterInput!) : LoginPayLoad!
    login(email: String!, password: String!) : LoginPayLoad!
    refresh: LoginPayLoad!
    logout: Boolean!
}